# ──────────────────────────────────────────────────────────────────────────────
# Requirements for:
#   Byzantine-Robust Federated Learning for ICS Intrusion Detection
#   (Massaoudi, Davis, Ez Eddin)
#
# Install:  pip install -r requirements.txt
# Run:      python main.py
#
# Python 3.9+ recommended.  All versions tested on Python 3.10 / Windows & Linux.
# ──────────────────────────────────────────────────────────────────────────────

# Deep learning
torch>=2.0.0,<3.0.0
torchvision>=0.15.0          # required by some torch builds; unused directly

# Data science
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

# Visualisation
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Explainability (optional — graceful fallback to attention weights if absent)
shap>=0.42.0
